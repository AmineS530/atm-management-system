# new
    make id unable to be a 0





change remaining funcs to work with sqlite
    - create account
        + prompt to fill records struct before inserting in db
        + functions to check each input

put password related funcs in one file to avoid confusion
    - wip

figure the use of stay or return and success
    - situational

update the header
    - lmfao forgor what to update

sanitize input and redo sqlite functions
    -no injection working for now

user in main function doesnt need to be a pointer
    - done

print error if no account for the user
    - wip

add logs
    -to-to do

clean up code
    - do what

remove extra library imports and create functions manually if possible
    - arry

check if accnbr already exist or not

problem with dates, fges and scan f interfere with each other, check country putting invalid everytime at the start
check account number not working 
+++++++++++++++++++++++++++++++++++++++++
update account info UI, and error message to be updated
