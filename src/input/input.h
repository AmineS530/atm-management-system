#ifndef INPUT_H
#define INPUT_H

#include "../helpers/helpers.h"

/* input_utls.c */
int check_phone_numb(char *phone);
int is_alpha(int c);
int is_digit(int c);
int check_country(char *country);

#endif